import styled from 'styled-components';

export const Description = styled.div`
    color: var(--gray700);
    font-size: 1.125rem;
    line-height: 160%;
`;

export const ContactButtonList = styled.ul`
    display: flex;
    align-items: center;
    gap: 12px;

    margin-top: 16px;
    margin-bottom: 0;
`;

export const TitleSpan = styled.span`
    font-size: 0.95em;
`;

export const Image = styled.img`
    width: 120px;
    height: 120px;

    border-radius: 4px;
`;
